A content-based movie recommender system built with TF-IDF and cosine similarity on movie genres.

Requirements:
- Python 3.x
- pandas
- scikit-learn

Install dependencies:
pip install pandas scikit-learn

Run:
python recommender.py
